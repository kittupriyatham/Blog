# Web Server
flask>=2.3.0,<4.0.0
gunicorn

# CSRF protection for forms
flask-wtf

# Database (Cosmos DB / MongoDB)
pymongo
dnspython

# Cloud Storage (Azure Blob Storage)
azure-storage-blob

# Environment & Utilities
python-dotenv
Werkzeug

# Local development fallback (in-memory DB when no MONGO_URI is set)
mongomock

# Text cards: render post text to an image for Instagram/Pinterest
Pillow>=10.0.0